# Accounting Book Web App

A modern React web application for personal accounting.

## features
(Pending detailed requirements from development specification)

## Technology Stack
- **Framework:** React + TypeScript (Vite)
- **Styling:** Vanilla CSS (as per project guidelines)
- **Deployment:** GitHub Pages (via GitHub Actions)
- **Package Manager:** npm

## Getting Started

### Prerequisites
- Node.js (v18 or higher recommended)
- npm

### Installation
```bash
npm install
```

### Development
Start the local development server:
```bash
npm run dev
```

### Build
Build the project for production:
```bash
npm run build
```

## Deployment
This project is configured to automatically deploy to GitHub Pages on every push to the `main` branch.
Workflow file: `.github/workflows/deploy.yml`

## Project Structure
- `src/`: Source code
- `public/`: Static assets
- `dist/`: Build output (not committed)
